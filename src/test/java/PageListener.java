public class PageListener {
    /**
     * This class helps find out the page changes
     * and whenever a page changes it will find out which page is that
     * and initiates an object of thet class
     * which will help execute the default takeAction on that class
     */
}
